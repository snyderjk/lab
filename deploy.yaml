apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: name
  name: name
spec:
  replicas: 10
  selector:
    matchLabels:
      app: name
  template:
    metadata:
      labels:
        app: name
    spec:
      containers:
        - image: httpd:alpine3.18
          name: httpd
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
